import:
  - atmos-pro/default.yaml

vars:
  namespace: cplive
  tenant: plat
  region: us-east-2
  environment: ue2
  github_repo_name: atmos-pro-with-ecs

terraform:
  # Configure context
  vars:
    label_order:
      - namespace
      - tenant
      - environment
      - stage
      - name
      - attributes
    descriptor_formats:
      account_name:
        format: "%v-%v"
        labels:
          - tenant
          - stage
      stack:
        format: "%v-%v-%v"
        labels:
          - tenant
          - environment
          - stage

  backend_type: s3
  backend:
    s3:
      bucket: cplive-plat-ue2-dev-tfstate-apps
      encrypt: true
      key: terraform.tfstate
      acl: bucket-owner-full-control
      region: us-east-2

  dependencies:
    tools:
      # Use a specific terraform version from toolchain
      opentofu: "1.10.7"
